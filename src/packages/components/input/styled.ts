import styled from 'styled-components';

const Input = styled.input`
  font-size: 18px;
  border-radius: 5px;
  padding: 5px 10px;
  outline: 0;
  width: 100%;
`;

const Container = styled.div`
  display: inline-flex;
  position: relative;
`;

export const Styled = {
  Input,
  Container,
};
