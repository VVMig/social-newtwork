import React from 'react';
import { Styled } from '../styled';
import { Info } from '../info/Info';

export const RightContent = () => {
  return (
    <Styled.RightContent>
      <Info />
    </Styled.RightContent>
  );
};
